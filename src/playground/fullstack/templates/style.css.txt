@import 'tailwindcss';
:root { color-scheme: light; font-family: 'Arial', 'Helvetica Neue', sans-serif; color: #102e2d; background: #eeede6; }
* { box-sizing: border-box; }
body { margin: 0; }
button, input, select { font: inherit; color: inherit; }
button, a { -webkit-tap-highlight-color: transparent; }
button { cursor: pointer; }
button:disabled { cursor: not-allowed; opacity: .45; }
a { color: inherit; text-decoration: none; }
button:focus-visible, a:focus-visible, summary:focus-visible, input:focus-visible, select:focus-visible { outline: 3px solid #4b9390; outline-offset: 5px; }
button { border: 0; background: none; }
h1, h2, h3, p, figure { margin: 0; }
html { scroll-behavior: smooth; scroll-padding-top: 24px; }
.store { --cream: #eeede6; --ink: #102e2d; --teal: #498a86; --line: #183c3a30; overflow: clip; background: var(--cream); color: var(--ink); }
.announcement { display: flex; justify-content: space-between; padding: 11px 4%; background: #123c37; color: #eeede6; font-size: 10px; letter-spacing: .06em; }
.site-header { display: flex; align-items: center; justify-content: space-between; gap: 24px; padding: 28px 4%; }
.wordmark { display: flex; align-items: center; gap: 12px; font-size: 15px; font-weight: 800; letter-spacing: -.04em; }
.brand-mark { display: flex; align-items: center; gap: 3px; }
.brand-mark i { display: block; width: 11px; height: 31px; background: currentColor; border-radius: 20px; transform: rotate(-13deg); }
.brand-mark i:nth-child(2) { height: 12px; }
.site-header nav { display: flex; gap: 28px; align-items: center; font-size: 12px; }
.site-header nav a:hover { text-decoration: underline; text-underline-offset: 5px; }
.bag-button { border-radius: 30px; padding: 12px 22px; background: var(--teal); color: #fff; }
.hero { padding: 20px 4% 0; }
.hero-topline, .hero-bottom { display: flex; justify-content: space-between; gap: 24px; font-size: 10px; letter-spacing: .06em; }
.hero-topline { padding: 20px 0; border-top: 1px solid var(--line); }
.hero-topline span:last-child { opacity: .55; }
.hero-layout { display: grid; grid-template-columns: 1fr 1.15fr; align-items: center; min-height: 650px; }
.hero-copy { position: relative; z-index: 1; padding: 32px 0 64px; }
.eyebrow { font-size: 9px; letter-spacing: .17em; font-weight: 600; }
h1 { margin: 24px 0; font-size: clamp(68px, 8.8vw, 150px); line-height: .89; letter-spacing: -.075em; font-weight: 800; }
h1 span, h1 em { display: block; }
h1 em { font-family: Georgia, serif; font-weight: 400; color: #43817b; letter-spacing: -.07em; }
.hero-description { max-width: 325px; font-size: 14px; line-height: 1.7; margin: 28px 0; }
.pill { display: inline-flex; align-items: center; justify-content: space-between; gap: 28px; border: 1px solid var(--ink); padding: 16px 23px; border-radius: 40px; font-size: 12px; line-height: 1.2; transition: background .2s, transform .2s; }
.pill:hover { background: #173c3510; transform: translateY(-2px); }
.pill.primary { background: var(--teal); color: #fff; border-color: var(--teal); }
.pill.primary:hover { background: #326f69; }
.hero-object { position: relative; align-self: stretch; display: flex; justify-content: center; flex-direction: column; margin: 0 -5% 0 -8%; }
.hero-object:before { content: ''; position: absolute; inset: 7% 0 13% 2%; border-radius: 50%; background: radial-gradient(ellipse, #d2d9ce 0%, #e3e5db 48%, transparent 69%); }
.product-art { display: block; width: 100%; height: auto; }
.hero-art { position: relative; width: 115%; margin-left: -4%; }
.arm-assembly { transform-origin: 50% 70%; animation: arm-sway 9s ease-in-out infinite alternate; }
@keyframes arm-sway { from { transform: rotate(-3deg) translateY(5px); } to { transform: rotate(3deg) translateY(-7px); } }
.store[data-motion='false'] .arm-assembly { animation-play-state: paused; }
.object-label { position: absolute; top: 10%; right: 9%; font-size: 9px; letter-spacing: .12em; transform: rotate(90deg); transform-origin: right; z-index: 1; }
.object-caption { position: relative; display: flex; justify-content: space-between; gap: 10px; font-size: 10px; width: 84%; align-self: center; padding-bottom: 24px; }
.motion-button { font-size: 10px; text-decoration: underline; text-underline-offset: 4px; }
.hero-bottom { border-top: 1px solid var(--line); padding: 22px 0; font-size: 9px; }
.intro-strip { display: grid; grid-template-columns: repeat(3, 1fr); padding: 38px 4%; border-block: 1px solid var(--line); background: #e4e6dc; }
.intro-strip > div { position: relative; padding: 0 30px 0 45px; }
.intro-strip > div + div { border-left: 1px solid var(--line); }
.intro-strip span { position: absolute; left: 15px; font-size: 9px; opacity: .5; }
.intro-strip p { font-size: 16px; letter-spacing: -.03em; margin-bottom: 8px; }
.intro-strip small { font-size: 10px; opacity: .65; line-height: 1.5; }
.collection { padding: 100px 4%; }
.section-heading { display: flex; justify-content: space-between; align-items: end; gap: 25px; margin-bottom: 45px; }
h2 { font-size: clamp(36px, 4.6vw, 75px); font-weight: 500; line-height: 1; letter-spacing: -.055em; }
.section-heading h2 { margin-top: 18px; }
.section-heading > p { font-size: 12px; line-height: 1.7; opacity: .65; }
.category-list { display: flex; gap: 8px; flex-wrap: wrap; margin-bottom: 25px; }
.category { display: flex; align-items: center; gap: 13px; border: 1px solid var(--line); border-radius: 30px; padding: 11px 16px; font-size: 11px; }
.category span { font-size: 9px; opacity: .55; }
.category[aria-pressed='true'] { background: #123c37; color: #f1eee4; border-color: #123c37; }
.category:hover:not([aria-pressed='true']) { background: #dce2d6; }
.catalog-tools { display: flex; justify-content: space-between; align-items: center; gap: 22px; padding: 16px 0; border-block: 1px solid var(--line); }
.search { display: flex; align-items: center; gap: 12px; flex: 1; }
.search > span { font-size: 24px; }
.search input { border: 0; outline-offset: 2px; background: transparent; padding: 9px 0; width: 100%; font-size: 12px; }
.budget { display: flex; align-items: center; gap: 14px; font-size: 10px; white-space: nowrap; }
.budget input { width: 95px; accent-color: var(--teal); }
.sort select { border: 0; background: transparent; padding: 8px; font-size: 11px; }
.results-line { display: flex; justify-content: space-between; min-height: 58px; align-items: center; font-size: 10px; color: #567068; }
.product-grid { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 40px 18px; }
.product-visual { position: relative; display: block; width: 100%; aspect-ratio: 1/1.05; overflow: hidden; border-radius: 5px; background: #e2e4d9; border: 1px solid #183c3a08; }
.product-card:nth-child(3n+2) .product-visual { background: #e8e2d7; }
.product-card:nth-child(3n+3) .product-visual { background: #dce3dc; }
.product-visual .product-art { transition: transform .6s cubic-bezier(.2,.7,.3,1); }
.product-visual:hover .product-art { transform: translateY(-8px) rotate(-3deg) scale(1.05); }
.product-code { position: absolute; left: 18px; top: 18px; font-size: 8px; letter-spacing: .07em; z-index: 1; opacity: .65; }
.product-badge { position: absolute; right: 15px; top: 15px; padding: 6px 9px; background: #eeede6c9; border-radius: 20px; font-size: 8px; z-index: 1; }
.product-explore { position: absolute; bottom: 15px; right: 15px; width: 30px; height: 30px; display: grid; place-items: center; border: 1px solid var(--line); border-radius: 50%; }
.product-info { padding: 17px 2px 0; }
.product-title { display: flex; justify-content: space-between; align-items: baseline; gap: 12px; font-size: 13px; }
.product-title button { text-align: left; font-weight: 600; padding: 0; letter-spacing: -.025em; }
.product-title > span { font-size: 11px; white-space: nowrap; }
.product-info > p { font-size: 10px; color: #63726b; margin: 9px 0 17px; }
.product-bottom { display: flex; justify-content: space-between; align-items: center; gap: 8px; padding-top: 14px; border-top: 1px solid var(--line); }
.product-bottom > span { font-size: 8px; text-transform: uppercase; letter-spacing: .09em; opacity: .6; }
.add-button { font-size: 10px; padding: 4px 0 4px 10px; white-space: nowrap; }
.add-button:hover { text-decoration: underline; text-underline-offset: 4px; }
.feature-story { display: grid; grid-template-columns: 1fr 1fr; background: #c4d6c9; padding: 80px 5%; gap: 7%; align-items: center; }
.story-art { position: relative; aspect-ratio: 1; }
.story-grid { position: absolute; inset: 0; border-radius: 50%; border: 1px solid #183c3a20; background-image: linear-gradient(#183c3a12 1px, transparent 1px),linear-gradient(90deg,#183c3a12 1px,transparent 1px); background-size: 40px 40px; }
.story-art .product-art { position: relative; transform: rotate(-8deg); }
.story-stamp { display: block; text-align: center; font-size: 8px; letter-spacing: .2em; }
.story-copy h2 { margin: 25px 0; }
.story-copy h2 em, .footer-top em { font-family: Georgia, serif; font-weight: 400; }
.story-copy > p:not(.eyebrow) { max-width: 415px; font-size: 13px; line-height: 1.8; margin-bottom: 18px; }
.story-copy .pill { margin-top: 16px; }
.starter-band { display: grid; grid-template-columns: 1fr 1fr; gap: 60px; align-items: center; padding: 90px 8%; }
.starter-band h2 { margin: 24px 0; }
.starter-band p:not(.eyebrow) { font-size: 13px; line-height: 1.7; max-width: 270px; margin-bottom: 26px; }
.starter-band > div:last-child { background: #e4d4b7; border-radius: 50% 50% 6px 6px; }
.questions { padding: 65px 8% 100px; border-top: 1px solid var(--line); }
.questions h2 { margin: 20px 0 40px; }
.question-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 0 60px; }
.questions details { border-top: 1px solid var(--line); padding: 21px 0; }
.questions summary { display: flex; justify-content: space-between; gap: 12px; font-size: 13px; cursor: pointer; list-style: none; }
.questions summary:after { content: '+'; }
.questions details[open] summary:after { content: '−'; }
.questions details p { font-size: 12px; line-height: 1.8; margin-top: 18px; opacity: .7; }
.site-footer { padding: 70px 4% 25px; background: #163e37; color: #e8e9da; }
.footer-top { display: flex; justify-content: space-between; align-items: center; gap: 30px; }
.footer-top p { font-size: clamp(38px, 5vw, 70px); line-height: 1.04; letter-spacing: -.04em; }
.footer-top .pill { border-color: #e8e9da60; }
.footer-wordmark { font-size: clamp(36px, 14vw, 220px); font-weight: 800; letter-spacing: -.075em; line-height: 1; margin: 65px 0 40px; white-space: nowrap; }
.footer-bottom { display: flex; justify-content: space-between; gap: 20px; border-top: 1px solid #e8e9da40; padding-top: 20px; font-size: 9px; }
.empty-state { text-align: center; padding: 80px 20px; border: 1px dashed var(--line); border-radius: 8px; }
.empty-state h3 { font-size: 26px; letter-spacing: -.04em; }
.empty-state p { margin: 18px auto; max-width: 500px; font-size: 13px; line-height: 1.6; }
.text-button { font-size: 11px; text-decoration: underline; text-underline-offset: 4px; padding: 8px 0; }
.toast, .error-toast { position: fixed; z-index: 20; bottom: 24px; left: 50%; transform: translateX(-50%); display: flex; align-items: center; gap: 20px; max-width: calc(100% - 30px); background: #163e37; color: #f5f1e4; padding: 14px 18px; border-radius: 10px; font-size: 12px; box-shadow: 0 6px 25px #0002; }
.toast[hidden] { display: none; }
.error-toast { background: #7d4132; bottom: 90px; }
.toast button, .error-toast button { color: inherit; padding: 0 4px; font-size: 22px; }
dialog { color: var(--ink); background: var(--cream); padding: 0; border: 0; border-radius: 10px; max-height: 90dvh; overflow: auto; box-shadow: 0 25px 100px #0003; margin: auto; }
dialog::backdrop { background: #153d3566; backdrop-filter: blur(8px); }
.dialog-close { width: 36px; height: 36px; display: grid; place-items: center; border: 1px solid var(--line); border-radius: 50%; font-size: 24px; }
.product-dialog { width: min(920px, 94vw); }
.product-dialog > .dialog-close { position: absolute; z-index: 2; top: 18px; right: 18px; background: var(--cream); }
.detail-layout { display: grid; grid-template-columns: 1fr 1fr; }
.detail-art { position: relative; background: #dce3d8; display: flex; align-items: center; }
.detail-copy { padding: 55px 35px 35px; }
.detail-copy h2 { font-size: 38px; margin: 20px 0; }
.detail-price { font-size: 20px !important; margin-bottom: 22px; }
.detail-copy > p { font-size: 12px; line-height: 1.7; }
.specs { padding: 0; list-style: none; margin: 22px 0; font-size: 11px; }
.specs li { padding: 12px 0; border-top: 1px solid var(--line); }
.specs li:before { content: '↗'; margin-right: 12px; color: var(--teal); }
.stock { font-size: 10px !important; margin-bottom: 18px; color: #43817b; }
.detail-copy .pill { width: 100%; justify-content: center; margin-bottom: 8px; }
.detail-footnote { font-size: 9px !important; opacity: .6; margin-top: 18px; }
.bag-dialog { width: min(690px, 95vw); }
.bag-header { display: flex; align-items: center; justify-content: space-between; gap: 15px; padding: 28px 32px; border-bottom: 1px solid var(--line); }
.bag-header h2 { font-size: 35px; margin-top: 12px; }
.bag-content { padding: 0 32px 32px; }
.bag-lines { list-style: none; margin: 0; padding: 0; }
.bag-line { display: flex; align-items: center; gap: 18px; border-bottom: 1px solid var(--line); padding: 20px 0; }
.bag-art { width: 95px; flex-shrink: 0; background: #dde2d5; border-radius: 5px; }
.bag-item-info { flex: 1; }
.bag-item-info h3 { font-size: 14px; font-weight: 500; }
.bag-item-info p { font-size: 11px; opacity: .65; margin-top: 7px; }
.bag-line > strong { font-size: 12px; font-weight: 500; }
.quantity-control { display: flex; gap: 10px; align-items: center; margin-top: 12px; font-size: 12px; }
.quantity-control button:not(.remove-button) { border: 1px solid var(--line); border-radius: 50%; width: 25px; height: 25px; }
.remove-button { text-decoration: underline; font-size: 9px; opacity: .65; margin-left: 3px; }
.bag-summary { padding-top: 24px; }
.delivery-choice { display: flex; align-items: center; justify-content: space-between; gap: 20px; font-size: 12px; margin-bottom: 20px; }
.delivery-choice select { background: transparent; border: 1px solid var(--line); padding: 10px; border-radius: 5px; max-width: 70%; font-size: 11px; }
.total-line { display: flex; justify-content: space-between; margin: 14px 0; font-size: 12px; }
.grand-total { border-top: 1px solid var(--line); padding-top: 20px; font-size: 20px; }
.demo-note { font-size: 10px; line-height: 1.6; opacity: .65; margin: 18px 0; }
.checkout-button { width: 100%; justify-content: center; }
.empty-bag, .order-confirmation { text-align: center; padding: 40px 35px; }
.empty-bag .product-art { max-width: 220px; margin: 0 auto; }
.empty-bag h3, .order-confirmation h3 { font-family: Georgia, serif; font-size: 34px; font-weight: 400; font-style: italic; margin: 15px 0; }
.empty-bag p, .order-confirmation p { font-size: 12px; line-height: 1.7; max-width: 360px; margin: 12px auto 22px; }
.confirmation-mark { display: grid; place-items: center; width: 65px; height: 65px; border-radius: 50%; background: #c7dbcc; font-size: 30px; margin: 0 auto 20px; }
.receipt-lines { text-align: left; margin: 25px 0; list-style: none; padding: 0; }
.receipt-lines li { display: flex; justify-content: space-between; gap: 20px; border-top: 1px solid var(--line); padding: 13px 0; font-size: 12px; }
.receipt-total { font-weight: 600; }
.inline-error { margin: 16px 25px; padding: 12px; border-radius: 5px; background: #eed5c8; color: #733d2d; font-size: 12px; line-height: 1.5; }
.inline-status { font-size: 11px; color: #326f69; }
@media (min-width: 1600px) { .collection, .hero, .site-header, .intro-strip { padding-left: max(4%, calc((100vw - 1500px)/2)); padding-right: max(4%, calc((100vw - 1500px)/2)); } }
@media (max-width: 900px) {
  .hero-layout { min-height: 540px; }
  .hero-description { max-width: 280px; font-size: 12px; }
  .hero-art { width: 130%; margin-left: -12%; }
  .object-caption { font-size: 9px; flex-direction: column; align-items: flex-start; }
  .product-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
  .intro-strip > div { padding-right: 15px; }
  .intro-strip small { display: block; }
  .budget { display: none; }
  .feature-story { gap: 5%; padding: 60px 4%; }
  .story-copy h2 { font-size: 48px; }
}
@media (max-width: 600px) {
  .announcement { font-size: 8px; padding: 10px 5%; }
  .site-header { padding: 22px 5%; gap: 15px; }
  .wordmark { font-size: 12px; gap: 7px; }
  .brand-mark i { width: 8px; height: 24px; }
  .site-header nav { gap: 10px; }
  .site-header nav a { display: none; }
  .bag-button { padding: 11px 15px; font-size: 10px; }
  .hero { padding: 0 5%; }
  .hero-topline { font-size: 8px; }
  .hero-topline span:last-child { display: none; }
  .hero-layout { grid-template-columns: 1fr; }
  .hero-copy { padding: 28px 0 0; }
  h1 { font-size: clamp(64px, 17vw, 100px); }
  .hero-description { max-width: 320px; }
  .hero-object { margin: -10px -7% 0; }
  .hero-art { width: 100%; margin-left: 0; }
  .object-label { right: 8%; top: 7%; font-size: 8px; }
  .object-caption { flex-direction: row; align-items: baseline; font-size: 8px; }
  .hero-bottom { font-size: 7px; padding: 18px 0; }
  .hero-bottom span:nth-child(2) { display: none; }
  .intro-strip { grid-template-columns: 1fr; gap: 20px; padding: 26px 5%; }
  .intro-strip > div { padding: 0 0 0 30px; }
  .intro-strip > div + div { border-left: 0; }
  .intro-strip span { left: 0; }
  .intro-strip p { font-size: 14px; margin-bottom: 4px; }
  .collection { padding: 60px 5%; }
  .section-heading { display: block; margin-bottom: 25px; }
  .section-heading > p { margin-top: 20px; font-size: 11px; }
  .section-heading > p br { display: none; }
  .category-list { gap: 6px; }
  .category { font-size: 9px; padding: 9px 12px; gap: 7px; }
  .catalog-tools { gap: 8px; }
  .sort select { padding: 5px 0; max-width: 130px; font-size: 9px; }
  .search { gap: 7px; min-width: 0; }
  .search input { font-size: 10px; min-width: 0; }
  .product-grid { gap: 30px 12px; }
  .product-code { left: 10px; top: 12px; font-size: 6px; }
  .product-badge { font-size: 6px; top: 9px; right: 7px; padding: 5px 6px; }
  .product-explore { width: 24px; height: 24px; right: 10px; bottom: 10px; }
  .product-title { flex-direction: column; gap: 6px; font-size: 12px; }
  .product-title > span { font-size: 10px; }
  .product-info > p { font-size: 9px; line-height: 1.5; min-height: 27px; }
  .product-bottom { align-items: flex-start; flex-direction: column; gap: 10px; }
  .add-button { padding-left: 0; font-size: 10px; }
  .feature-story, .starter-band { grid-template-columns: 1fr; gap: 40px; padding: 50px 7%; }
  .story-art { order: 2; max-width: 370px; }
  .story-copy h2 { font-size: 50px; }
  .questions { padding: 50px 7%; }
  .question-grid { grid-template-columns: 1fr; }
  .site-footer { padding: 45px 5% 24px; }
  .footer-top { align-items: flex-start; flex-direction: column; }
  .footer-wordmark { font-size: 13vw; margin: 45px 0 24px; }
  .footer-bottom { flex-direction: column; gap: 12px; font-size: 8px; }
  .detail-layout { grid-template-columns: 1fr; }
  .detail-art .product-art { width: 70%; margin: auto; }
  .detail-copy { padding: 28px; }
  .detail-copy h2 { font-size: 32px; }
  .bag-header { padding: 22px; }
  .bag-content { padding: 0 20px 25px; }
  .bag-line { gap: 10px; flex-wrap: wrap; }
  .bag-art { width: 65px; }
  .bag-item-info h3 { font-size: 12px; }
  .bag-line > strong { margin-left: auto; }
  .quantity-control { gap: 8px; }
  .empty-bag, .order-confirmation { padding: 30px 24px; }
}
@media (prefers-reduced-motion: reduce) {
  html { scroll-behavior: auto; }
  *, *:before, *:after { animation: none !important; transition: none !important; }
}
